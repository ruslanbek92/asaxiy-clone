function MainSection({ children }) {
    return (
        <section className="position: relative h-screen mt-[603px]  md:mt-36">
            {children}
        </section>
    );
}
export default MainSection;
